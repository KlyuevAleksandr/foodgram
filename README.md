# Foodgram - «Продуктовый помощник»

Онлайн-сервис и API для публикации рецептов. Пользователи могут создавать свои кулинарные рецепты, добавлять их в избранное, подписываться на других авторов и формировать список покупок для выбранных рецептов.

## Основные возможности

### Для пользователей
- **Рецепты**: Публикация, редактирование и просмотр рецептов
- **Избранное**: Добавление рецептов в персональную коллекцию
- **Подписки**: Подписка на интересных авторов и отслеживание их новых рецептов
- **Список покупок**: Формирование списка ингредиентов для выбранных рецептов
- **Теги**: Фильтрация рецептов по тегам (завтрак, обед, ужин)

### Для разработчиков
- **REST API**: Полнофункциональное API с документацией
- **Аутентификация**: Token-based аутентификация
- **Пагинация**: Постраничный вывод результатов
- **Фильтрация**: Гибкая система фильтрации рецептов

## Технологический стек

### Backend
- **Python 3.11+** - основной язык программирования
- **Django 4.2** - веб-фреймворк
- **Django REST Framework** - REST API
- **PostgreSQL** - база данных
- **Docker** - контейнеризация
- **Nginx** - веб-сервер
- **Gunicorn** - WSGI-сервер

### Frontend
- **HTML/CSS/JavaScript** - базовая фронтенд-часть

## Установка и запуск

### Требования
- Docker
- Docker Compose

### Быстрый старт

1. **Клонирование репозитория**
```bash
git clone 
```
```
cd foodgram
```
2. **Настройка переменных окружения**
* Создайте файл .env в корневой директории:
```env
DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
SECRET_KEY=your-secret-key-here
DEBUG=False
```

3. **Запуск приложения**
```bash
docker-compose up -d --build
```

4. **Миграции и статические файлы**
```bash
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py collectstatic --noinput
```

5. **Создание суперпользователя (опционально)**
```bash
docker-compose exec backend python manage.py createsuperuser
```

6. **Доступ к приложению**
* Веб-приложение: http://localhost

* Админ-панель: http://localhost/admin

* API документация: http://localhost/api/docs/

## 📚 API Endpoints
### Аутентификация
POST /api/auth/token/login/ - получение токена

POST /api/auth/token/logout/ - выход

### Пользователи
GET /api/users/ - список пользователей

POST /api/users/ - регистрация

GET /api/users/{id}/ - профиль пользователя

GET /api/users/me/ - текущий пользователь

### Подписки
GET /api/users/subscriptions/ - мои подписки

POST /api/users/{id}/subscribe/ - подписаться

DELETE /api/users/{id}/subscribe/ - отписаться

### Рецепты
GET /api/recipes/ - список рецептов

POST /api/recipes/ - создать рецепт

GET /api/recipes/{id}/ - детали рецепта

PATCH /api/recipes/{id}/ - обновить рецепт

DELETE /api/recipes/{id}/ - удалить рецепт

### Избранное
GET /api/recipes/favorites/ - избранные рецепты

POST /api/recipes/{id}/favorite/ - добавить в избранное

DELETE /api/recipes/{id}/favorite/ - удалить из избранного

### Список покупок
GET /api/recipes/download_shopping_cart/ - скачать список

POST /api/recipes/{id}/shopping_cart/ - добавить в список

DELETE /api/recipes/{id}/shopping_cart/ - удалить из списка

### Модели данных

## Основные модели
User - пользователи системы

* Recipe - рецепты

* Ingredient - ингредиенты

* Tag - теги рецептов

* RecipeIngredient - связь рецептов и ингредиентов

* Subscription - подписки пользователей

* Favorite - избранные рецепты

* ShoppingCart - список покупок

## Аутентификация
### Проект использует token-based аутентификацию. Для доступа к защищенным endpoint'ам необходимо:

### Получить токен через /api/auth/token/login/

### Добавить заголовок в запросы:

```text
Authorization: Token <your-token>
```

## Docker
Проект полностью контейнеризирован:

* backend - Django приложение

* frontend - статические файлы

* db - PostgreSQL база данных

* nginx - веб-сервер

## Структура проекта
```text
foodgram/
├── backend/                 # Django приложение
│   ├── apps/
│   │   ├── users/          # приложение пользователей
│   │   ├── recipes/        # приложение рецептов
│   │   └── api/            # API endpoints
│   ├── config/             # настройки проекта
│   └── requirements.txt    # зависимости Python
├── frontend/               # статические файлы
├── nginx/                  # конфигурация nginx
├── docker-compose.yml      # docker compose
└── README.md              # документация
```

Яндекс.Практикум - образовательная платформа